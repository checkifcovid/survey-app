import React, { useState } from 'react';
import Navigation from './navigation';

export default {
    title: 'Navigation',
};

export const textInput = () => {
    return (
        <Navigation
        />
    )
};
